export const Order = {
  state: {
    list: [],
  },
  effects: dispatch => ({
    fetchOrdersList(payload, state) {},
    doOrder(payload, state) {},
    cancelOrder(payload, state) {},
    acceptOrder(payload, state) {},
    editOrder(payload, state) {},
  }),
};
